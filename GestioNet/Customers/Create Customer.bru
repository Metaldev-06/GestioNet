meta {
  name: Create Customer
  type: http
  seq: 1
}

post {
  url: {{URL}}/customers
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "name": "Macon SRL"
  }
}
